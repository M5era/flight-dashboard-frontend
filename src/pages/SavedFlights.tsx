
const SavedFlights = () => {
  // This is a placeholder for now.
  // We will fetch and display saved flights here.
  return (
    <div style={{ padding: 20 }}>
      <h2 style={{ textAlign: 'center' }}>My Saved Flights</h2>
      <p style={{ textAlign: 'center' }}>Saved flights will be displayed here.</p>
    </div>
  );
};

export default SavedFlights;
